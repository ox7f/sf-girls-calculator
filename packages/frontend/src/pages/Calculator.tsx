import { Agents, Options, Result } from '../components';

const Calculator: React.FC = () => {
  return (
    <main>
      <Options />
      <Result />
      <Agents />
    </main>
  );
};

export default Calculator;
