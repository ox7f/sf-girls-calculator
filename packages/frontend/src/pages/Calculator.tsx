import { Agents, Targets, Result } from '../components';

const Calculator: React.FC = () => {
  return (
    <main>
      <Targets />
      <Result />
      <Agents />
    </main>
  );
};

export default Calculator;
